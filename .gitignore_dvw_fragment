__pycache__/
*.pyc
.pytest_cache/

# The generated knowledge base. Metric definitions only -- no player
# data -- but it is a runtime artifact that churns on every merge, so
# it is kept out of the repo rather than committed.
volley_kb_data.json

# Belt and braces: match files must never land in this public repo.
*.dvw
